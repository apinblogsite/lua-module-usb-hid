# Patch: components/common/app_claw/Kconfig
#
# Tambahkan blok berikut setelah blok CONFIG_APP_CLAW_LUA_MODULE_BLE_HID
# (sekitar baris 294-305).
#
# ─── Tambahkan setelah blok BLE_HID ─────────────────────────────────────────

        config APP_CLAW_LUA_MODULE_USB_HID
            bool "Enable USB HID host Lua module"
            default n
            depends on APP_CLAW_CAP_LUA
            depends on USB_HOST_ENABLED
            help
                Enable the USB HID host Lua module. Allows the ESP32-P4 to
                receive input reports from keyboards, mice, gamepads, and other
                HID peripherals connected to the USB-A port.
                Only supported on ESP32-P4. Requires CONFIG_USB_HOST_ENABLED.
                The USB port cannot be used as USB-JTAG console at the same time;
                make sure CONFIG_ESP_CONSOLE_UART=y in the board sdkconfig.
